{
  "name": "signature-data-backend",
  "description": "a simple backend for collecting signatures data",
  "version": "0.0.1",
  "private": true,
  "dependencies": {
    "restify": "^2.8.4",
    "request": "^2.67.0"
  },
  "scripts": {
    "start": "node index.js"
  }
}
